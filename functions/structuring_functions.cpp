#include <iostream>

using namespace std;

// function prototype
// identifiers may not exist
double compute_average(int, int, int)

init main()

{
    int score1 = 100, score2 = 91, score3 = 72;


    cout << "Score 1 = " << score1 << endl;
    cout << "Score 2 = " << score2 << endl;
    cout << "Score 3 = " << score3 << endl;


    double average = compute average(score1,score2,score3);
    cout << "The average is " << average << endl


    cout << "Score 1 = " << score1 << endl;
    cout << "Score 2 = " << score2 << endl;
    cout << "Score 3 = " << score3 << endl;

    double compute_average(int s1, int s2, int s3)

    {
        return (s1 + s2 + s3) / 3.0;
    }

    return 0;
}
